const {
    getRows,
    updateRows,
    addRows
} = require("./lib/read-write");
const {
    clearSheets,
    seedSheets
} = require("./lib/seeds");

module.exports = {
    getRows,
    updateRows,
    addRows,
    clearSheets,
    seedSheets
};
